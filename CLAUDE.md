# Teste-

App React Native (Expo) de análise de convergência multi-escola para ativos da
B3. Código em `src/`, ponto de entrada em `App.tsx`.

## Contexto do usuário

Antes de responder, leia `base-de-conhecimento/CLAUDE.md`. Ele diz o que
carregar de `base-de-conhecimento/01-knowledge/` — perfil, preferências,
decisões já tomadas — e como alimentar a base com o que aparecer nesta conversa.
